@props([
    'title' => 'Quick Menu',
    'subtitle' => 'Order your favorites with one tap.',
])

<section class="px-4 py-4">
    <h2 class="font-headline text-2xl font-bold tracking-tight text-primary">{{ $title }}</h2>
    <p class="text-on-surface-variant text-xs mt-1">{{ $subtitle }}</p>
</section>
